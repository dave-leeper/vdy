module.exports = class Messages {
    static SERVER_BEFORE_START = `SERVER_BEFORE_START`
    static SERVER_AFTER_START = `SERVER_AFTER_START`
    static HEARTBEAT = `HEARTBEAT`
    static NEW_LOG = `NEW_LOG`
}